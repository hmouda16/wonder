<header class="d-flex flex-row-nowrap justify-content-center p-20">
	<div class="content flex-fill d-flex flex-row-nowrap align-item-center justify-content-between">
		<a class="header-logo" href="{{path('home')}}">
			<img src="{{ asset('images/logo.svg')}}" alt="">
		</a>
		<div class="header-search p-10">
			<i class="fa-solid fa-magnifying-glass"></i>
			<input type="text">
		</div>
		{%- if is_granted('IS_AUTHENTICATED_FULLY') -%}
			<div class="d-flex flow-row-nowrap justify-content-center ">
				<a href="{{path('current_user')}}">
					<img src="{{app.user.picture}}" class="avatar avatar-lg mr-10" alt="">
				</a>
				<small class="d-flex flex-column justify-content-center">
					<a href="{{path('logout')}}">Déconnexion</a>
				</small>
			</div>
		{% else %}

			<div>
				<a href="{{path('signup')}}" class="btn  btn-primary mr-5">inscription</a>
				<a href="{{path('login')}}" class="btn  btn-primary-stroke">Conexion</a>
			</div>

		{%- endif -%}

	</div>
</header>
